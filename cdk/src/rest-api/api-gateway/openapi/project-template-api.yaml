openapi: 3.0.0

info:
  title: ${ProjectName} API
  description: Analytics Api
  version: '1.0'

tags:
  - name: Analytics Microservice
    description: Quicksight related endpoints

paths:
  $include: ./paths.yaml

components:

  schemas:
    $include: ./schemas/api-messages.yaml
    $include: ./schemas/project-template.yaml

